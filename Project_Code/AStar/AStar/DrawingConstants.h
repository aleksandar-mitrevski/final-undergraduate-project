#ifndef DRAWING_CONSTANTS_H
#define DRAWING_CONSTANTS_H

//window width in pixels
const int WINDOW_WIDTH = 640;

//window height in pixels
const int WINDOW_HEIGHT = 480;

//view angle for the window
const float VIEW_ANGLE = 45.0f;

//screen depth
const float Z_NEAR = 1.0f;
const float Z_FAR = 1000.0f;

//ASCII code of the escape key
const int KEY_ESCAPE = 27;

//window width
const float X_RANGE = 11.0f;

//window height
const float Y_RANGE = 8.0f;

//uppermost point of the window
const float UPPER_Y_POINT = 4.0f;

//leftmost point of the window
const float LEFT_X_POINT = -5.5f;

//obstacle delimiter in the logical grid
const double OBSTACLE_DELIMITER = 100.0;

#endif